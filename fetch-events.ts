import { initializeApp } from 'firebase/app';
import { getFirestore, doc, getDoc } from 'firebase/firestore';

// Need to match firebase config
const firebaseConfig = {
  // Can get from .env or just hardcode the test
};
